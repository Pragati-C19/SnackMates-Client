// Base URL

const snackmates_base_url = 'http://localhost:4000'; // Backend API URL
const authUrl = `${snackmates_base_url}/auth`; // Backend auth API URL
const restaurantsUrl = `${snackmates_base_url}/restaurants`; // Backend restaurants API URL
const menuUrl = `${snackmates_base_url}/menus`; // Backend menus API URL
const favoritesUrl = `${snackmates_base_url}/favorites`; // Backend favorites API URL
const cartURL = `${snackmates_base_url}/cart`; // Backend cart API URL

export default snackmates_base_url